materias = int(input("Ingrese su número de materias: "))
nombrematerias = []
nombredias = []
dificil = []
mediano = []
facil = []
materias_horas = []
i=1 
t=0


while i <= materias:
    nombre = input(f"Ingrese el nombre de la materia({i}): ")
    nombrematerias.append(nombre)
    dificultad = int(input( f"Qué tan difícil es la materia {nombre}? (1 si le cuesta poco, 2 si le cuesta mucho, 3 si le cuesta demasiado): "))
    if dificultad==1:
          facil.append(nombre)
    if dificultad==2:
          mediano.append(nombre)
    if dificultad==3:
          dificil.append(nombre)
          
    i+= 1
dias=int(input("Ingrese el número de días que puede dedicar por semana: "))
d=1
while d<=dias:
    dia = input(f"Escriba el nombre del dia {d}:")
    nombredias.append(dia)
    d+=1
horas=int(input("Ingrese el número de horas que puede dedicar a la semana: "))

 

def horas_por_dia(horas,dias):
    horas_por_dia= horas // dias
    return horas_por_dia

while t <= dias:
    if t==1:
        dia1=horas_por_dia(horas,dias)
    if t==2:
            dia2=horas_por_dia(horas,dias)
    if t==3:
            dia3=horas_por_dia(horas,dias)
    if t==4:
            dia4=horas_por_dia(horas,dias)
    if t==5:
            dia5=horas_por_dia(horas,dias)
    if t==6:
            dia6=horas_por_dia(horas,dias)
    
    if t==7:
            dia7=horas_por_dia(horas,dias)
    t+=1


if horas % dias !=0:
    dia1=horas_por_dia(horas,dias)+ horas % dias
    
def horas_por_materia(horas,materias):
    horas_por_materia = horas // materias
    return horas_por_materia

while horas>0:
    for nombre in dificil:
            materias_horas.append([nombre,3])
            horas=-3
    for nombre in mediano:
          materias_horas.append([nombre,2])
          horas=-1
    for nombre in facil:
          materias_horas.append([nombre,1])
          horas=-1
#Aqui ya estoy empezando a asignar horas, esta parte asignará cierto número
# de horas de cajón ahora me falta que el programa reparta las que le sobren. 
    
      

                                                                                                                                                        

    


#Ahora tengo que poner las materias y sus horas en los respectivos días que el usuario me dio.
 #while t<=horas_por_dia:
print(facil,mediano,dificil,nombrematerias,nombredias,materias_horas)

